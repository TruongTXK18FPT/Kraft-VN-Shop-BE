package com.mss301.kraft.product_service.enums;

/**
 * Common helmet sizes. Kept as an enum for validation/UI hints.
 * Persist product variant size as String to allow custom values when needed.
 */
public enum HelmetSize {
    XS,
    S,
    M,
    L,
    XL,
    XXL
}
